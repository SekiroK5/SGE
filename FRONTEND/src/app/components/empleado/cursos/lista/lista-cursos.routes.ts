import { Routes } from '@angular/router';
import { ListaCursosComponent } from './lista-cursos.component';

export const LISTA_CURSOS_ROUTES: Routes = [
  { path: '', component: ListaCursosComponent }
];