import { Routes } from '@angular/router';
import { EdicionEmpleadoComponent } from './edicion-empleado.component';

const EDICION_ROUTES: Routes = [
  { path: '', component: EdicionEmpleadoComponent }
];

export default EDICION_ROUTES;